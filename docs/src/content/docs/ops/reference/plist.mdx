---
title: plist.*
description: "plist.* ops — NSKeyedArchiver binary plist parsing."
sidebar:
  label: plist
---

plist.* ops — NSKeyedArchiver binary plist parsing.

Used by browser skills to parse keychain-stored NSKeyedArchiver blobs.
Handlers live in agentos-exec-executors.

## `plist.parse`

Parse a hex-encoded NSKeyedArchiver binary plist.

| Metadata | Value |
|---|---|
| **Capability** | `plist` |

### Request

| Field | Type | Required | Default | Description |
|---|---|---|---|---|
| `hex_data` | `string` | **yes** |  | Hex-encoded plist bytes. |
| `extract` | `map<string,u64>` | **yes** |  | Extraction map: field_name → object_index in the archived graph. |

### Response

Returns `map<string,json>` — Extracted fields as field_name → value.

### Notes

```text
Parse a hex-encoded NSKeyedArchiver binary plist and extract specific
object indices.

Args:
    hex_data: The plist as hex.
    extract: Mapping of {field_name: object_index} in the archived graph.

Returns:
    Dict of {field_name: value}.
```
