# A password for a caller-supplied identifier (credential provision).
description: >-
  Credential provision — a password for a caller-supplied identifier.
  Same matchmaking path as login_credentials; secret material never
  transits the LLM.
params:
  identifier: { type: "string", doc: "The account/identifier the password belongs to." }
