# Bare-preposition link `under` (Pattern 2) — multi-target stative.
# Storage label: `under`. Per-shape target lives in each shape's `links:` block.
# Sources: fare, membership, pass, seatmap, tax_line
# Per-shape targets: product, reservation
preposition: under
to: node
