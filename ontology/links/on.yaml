# Bare-preposition link `on` (Pattern 2) — multi-target stative.
# Storage label: `on`. Per-shape target lives in each shape's `links:` block.
# Sources: email, organization, tool_call, video, website
# Per-shape targets: channel, domain, product
preposition: "on"
to: node
