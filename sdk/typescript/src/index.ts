/**
 * agentOS App SDK
 *
 * Typed shape interfaces for building TypeScript apps that talk to the graph,
 * plus typed request/response shapes for the engine op contract.
 * Both are generated from the same YAML the Skills SDK is built from.
 *
 * Usage:
 *   import { Book, Person, Event } from '@agentos/sdk'
 *   import { ShellRunRequest, OpContracts } from '@agentos/sdk'
 */

export * from './shapes'
export * from './ops'
